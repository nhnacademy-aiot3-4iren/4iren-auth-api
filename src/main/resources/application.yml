# .yml (.yaml)
spring:
  application:
    # 본인 프로젝트 이름으로 변경(4iren-~~)
    name: 4iren-auth
  config:
    # 로컬 환경에서 8888 포트로 config server를 사용한다고 가정
    # 다른 포트를 쓰면 해당 포트로 적기
    import: ${CONFIG_SERVER_URL:optional:configserver:http://localhost:8888}
